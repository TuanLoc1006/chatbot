<?php
if(isset($_POST['train_rasa'])) {
    // Thực thi lệnh để huấn luyện Rasa và lưu kết quả vào biến $output
    exec("python p/t.py", $output);

    // In ra kết quả trên trang web
    echo "Rasa is being trained...<br>";
    echo "Training output:<br>";
    foreach ($output as $line) {
        echo $line . "<br>";
    }
}
?>
